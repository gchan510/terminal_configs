Detect vim version and features and change plugins based on that

Update install.sh
  Install taskwarrior and timewarrior, and their configs
  Change install script to check everything and read options before running anything
  Install powerline-fonts
